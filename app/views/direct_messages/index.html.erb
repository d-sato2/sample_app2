<% provide(:title, 'All users') %>
<h1>The users who exchanges messages</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 52 %>
      <%= link_to user.name, "http://localhost:3000/direct_messages/#{user.id}" %>
    </li>
  <% end %>
</ul>